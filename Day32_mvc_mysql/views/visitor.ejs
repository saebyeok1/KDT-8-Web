<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <title>방명록</title>
  </head>
  <body>
    <fieldset>
      <legend>방명록 등록</legend>
      <input type="text" placeholder="사용자 이름" id="userId" /><br />
      <input type="text" placeholder="방명록" id="content" /><br />
      <button type="button" onclick="postVisitors()">등록</button>
    </fieldset>
    <table border="1" cellspacing="0">
      <thead>
        <tr>
          <th>ID</th>
          <th>작성자</th>
          <th>방명록</th>
          <th>수정</th>
          <th>삭제</th>
        </tr>
      </thead>
      <tbody>
        <% for (let i =0; i < data.length; i++) { %>
        <tr id="tr_<%= data[i].id %>">
          <td><%= data[i].id %></td>
          <td><%= data[i].name %></td>
          <td><%= data[i].comment %></td>
          <td><button type="button">수정</button></td>
          <td><button type="button">삭제</button></td>
        </tr>
        <% } %>
      </tbody>
    </table>
    <script>
      axios;
    </script>
  </body>
</html>
